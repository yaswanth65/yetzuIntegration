import LegalPages from "@/components/ui/LegalPages";
import { codeOfConductPageData } from "../utils";

export default function TermsPage() {
    return <LegalPages data={codeOfConductPageData} />;
}
